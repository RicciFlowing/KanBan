<div class="row">
  <div class="col-md-4 col-md-offset-4 new form-inline"> Neue Notiz:
<%= form_for(@new_note, remote: true) do |f| %>
  <% if @new_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text, class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_field :date, class:"form-control date" %>
  </div>
  <div class="actions">
    <%= f.submit "Neue Notiz erstellen", class:"btn btn-default new-send" %>
  </div>
<% end %>
  </div>
</div>
